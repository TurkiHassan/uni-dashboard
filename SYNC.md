# مزامنة لوحة الدراسة

تسلسل النشر المقترح:

1. افتح جلسة Blackboard المسجلة في Chrome وتأكد من بقائها صالحة.
2. شغّل `python scrape.py` لاستخراج البيانات.
3. شغّل `python validate_data.py` كحاجز نشر.
4. لا تُستبدل `data.js` إذا فشل التحقق؛ احتفظ بآخر نسخة صالحة.
5. بعد نجاح التحقق، نفّذ commit وpush ثم انتظر تحديث GitHub Pages.

## قاعدة فشل مهمة

فشل Blackboard أو انتهاء الجلسة لا يعني عدم وجود واجبات. يجب أن تُعرض حالة المزامنة كفشل أو بيانات قديمة، مع الاحتفاظ بالبيانات السابقة بدل نشر قوائم فارغة.

## اختبار سريع

```powershell
python validate_data.py
```

يجب أن ينتهي الأمر بالرسالة `VALID` قبل أي نشر آلي.
